<tr>
    <td>{{ loop.index }}</td>
    <td>{{ membership.name }}</td>
    <td>{{ membership.duration }} Dni</td>
    <td>{{ membership.type }}
        <a href="{{ path('membership_edit', {id:membership.id}) }}"> aa</a>
    </td>
    <td>{{ membership.price }} PLN</td>
    <td>
        &nbsp;<a href="#"
                 class="js-delete-rep-log"
                data-url="{{ path('membership_type_delete', {id:membership.id}) }}">
            <span class="fa fa-trash"></span>
        </a>
    </td>
    <td>
        {# to niżej wyzuca membership edit form #}
        {#<a href="{{ path('membership_edit', {id:membership.id}) }}"#}
        <a href="#"
                 class="js-edit-rep-log"
                 data-url="{{ path('membership_edit', {id:membership.id}) }}">
            <span class="fa fa-pencil" data-toggle="modal" data-target="#myModal"></span>
        {#<div id="myModal">
            <span class="fa fa-pencil"></span>

        </div>#}

        </a>

    </td>
</tr>

