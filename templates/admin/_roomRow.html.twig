<tr>
    <td>{{ room.id }}</td>
    <td>{{ room.name }}</td>
    <td>{{ room.maxNumberOfUsers }}</td>
    <td>
        &nbsp;<a href="#"
                 class="js-delete-rep-log"
                 data-url="{{ path('room_delete', {id:room.id}) }}">
            <span class="fa fa-trash"></span>
        </a>
    </td>
    <td>
        &nbsp;<a href="#"
                 class="js-edit-rep-log"
                 data-url="{{ path('room_delete', {id:room.id}) }}">
            <span class="fa fa-pencil"></span>
        </a>
    </td>
</tr>